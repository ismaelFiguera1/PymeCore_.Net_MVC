@model PymeCore.ViewModels.Presupuestos.PresupuestoFormViewModel

@{
    ViewData["Title"] = "Editar presupuesto";
}

<article class="card" style="max-width: 600px;">
    <header class="card-header">
        <h2 class="card-title mb-0">Editar presupuesto</h2>
    </header>
    <form asp-action="Edit" asp-route-id="@Model.Id" method="post">
        <input asp-for="Id" type="hidden" />
        <section class="card-body">
            <div class="mb-3">
                <label asp-for="Numero" class="form-label"></label>
                <input asp-for="Numero" class="form-control" readonly />
            </div>
            <div class="mb-3">
                <label asp-for="ClienteId" class="form-label"></label>
                <select asp-for="ClienteId" asp-items="ViewBag.Clientes" class="form-select">
                    <option value="">— Selecciona un cliente —</option>
                </select>
                <span asp-validation-for="ClienteId" class="text-danger small"></span>
            </div>
            <div class="mb-3">
                <label asp-for="Observaciones" class="form-label"></label>
                <textarea asp-for="Observaciones" class="form-control" rows="3"></textarea>
                <span asp-validation-for="Observaciones" class="text-danger small"></span>
            </div>
        </section>
        <footer class="card-footer">
            <button type="submit" class="btn btn-primary btn-sm">
                <i class="bi bi-check-lg me-1"></i> Guardar
            </button>
            <a asp-action="Index" class="btn btn-outline-secondary btn-sm ms-1">
                <i class="bi bi-x-lg me-1"></i> Cancelar
            </a>
        </footer>
    </form>
</article>

@section Scripts {
    @{ await Html.RenderPartialAsync("_ValidationScriptsPartial"); }
}
